---
"mattpocock-skills": patch
---

Add the `submit` skill (engineering bucket, user-invoked). Run in the implement session before clearing context: it pushes the current branch to origin and opens one pull request on GitHub or merge request on GitLab, with a body generated by the `pr` skill plus `Closes #<n>` references for the linked issues. It guards the default branch and dirty tree, stops on a non-fast-forward without force pushing, resolves issue identity from arguments then session context then confirmed branch-name digits, shows a confirmation gate before any write, and never merges or waits on checks. Closes #4.
